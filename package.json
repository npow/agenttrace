{
  "name": "agenttrace-monorepo",
  "private": true,
  "workspaces": [
    "packages/viewer",
    "views/process-monitor"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
